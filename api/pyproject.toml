[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501", "N818"]

[tool.ruff.lint.isort]
known-first-party = ["config", "database", "routers", "middleware", "models", "services", "domain", "repositories", "tasks", "core", "schemas"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = "--cov=. --cov-report=term-missing --cov-fail-under=80"
filterwarnings = ["ignore::DeprecationWarning"]

[tool.coverage.run]
omit = ["tests/*", "alembic/*", "*/migrations/*", "tasks/ingest_task.py", "tasks/ping_task.py", "tasks/balance_task.py", "tasks/leak_detection_task.py", "models/leak.py"]
